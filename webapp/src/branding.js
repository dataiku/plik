// Branding Configuration
// Edit these values to customize the look and feel of your Plik instance.

export const branding = {
    // ── Logo ──
    // The name displayed in the header and page title
    name: 'Plik',

    // ── Background ──
    // Set to an image path (relative to /public) or '' to use the default dark theme.
    // The classic Plik background is '/img/background.jpg'
    backgroundImage: '',

    // Fallback color shown while the background image loads (or if none is set)
    backgroundColor: '',

    // Overlay opacity (0–1) — darkens the background image to keep text readable
    // Use 0 for no overlay, 0.6–0.8 for a subtle dark tint
    overlayOpacity: 0.2,
}
